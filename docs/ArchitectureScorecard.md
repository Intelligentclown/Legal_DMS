# Architecture Scorecard

**Last Updated:** 2026-08-22 (capability rows below) — see the 2026-08-31 staleness disclosure
immediately following this header block for what has changed since and has **not** been reassessed.
**Current Project Version:** 0.3.1 (see [docs/CHANGELOG.md](CHANGELOG.md)'s versioning note — not
bumped as part of this reassessment; CHANGELOG/release-note sync remains deferred, see T80's
narrowed scope below)
**Current Stage:** Stage 3 — Authentication & Authorization (in progress as of 2026-08-22; see the
staleness disclosure below for its current status). `T41`–`T78` are Done and
merged; `T79` (verification-only) is closed as **INCOMPLETE / NOT VERIFIED**, explicitly not a PASS;
`T80` (this reassessment) is in progress; `T81` (unrelated README cleanup) and `T82` (Electron-runtime
live smoke verification) are reserved task IDs, **not authorized**. See
[PROJECT_STATE.json](../PROJECT_STATE.json) for the machine-readable snapshot and
[IMPLEMENTATION_QUEUE.md](../IMPLEMENTATION_QUEUE.md) for the full task-by-task record.

**Staleness disclosure (2026-08-31, Documentation Manager, post-`T103` synchronization, GitHub Issue
#167):** the header block above is demonstrably stale relative to the current repository and is
corrected here at the pointer level only — **no capability row below has been reassessed by this
pass**, and no new architectural assessment is authorized or performed by it (that remains a
Software-Architect/QA-gated exercise, as `T80` itself was). What has actually happened since
2026-08-22: `T80` (this reassessment) completed and merged; `T81` (README cleanup) completed and
merged; `T82` was authorized, executed against the actual native Electron runtime, and closed
**FAIL** (session did not survive reload/restart), QA-Approved-with-comments; `T83`–`T85` closed
that finding out (test-account provisioning, the session-restoration fix, and a preload-script fix
blocking that fix's own verification) — Stage 3 (`T41`–`T85`) is now complete in full, not merely
`T41`–`T78`. `T86`–`T103` then performed pre-Stage-4 governance/architecture groundwork, not further
Stage 3 implementation: ten of the specification's twenty Required ADRs are now resolved (up from
zero at this file's last reassessment), plus two ADRs outside that original list (`ADR/0031`,
`ADR/0032`) deciding User↔Organization membership/tenant-context semantics and its pre-existing-data
reconciliation. See `docs/ProjectStatus.md`'s "Completed — Governance & Required-ADR Resolution
Series" section for the full record. **Whether any of this changes a capability row's status,
maturity symbol, or the Overall Architecture Health rating below is exactly the kind of judgment a
full reassessment makes — this disclosure does not make that judgment, and a fresh reassessment pass
(the next natural candidate being one gated on the eventual Organization/Tenant Core re-authorization
this ADR series feeds into) remains a separate, not-yet-authorized task.**

**Terminology note (corrects this file's prior revision):** `docs/ImplementationLog/Stage4/` names
its own directory "Stage 4," and this file's 2026-08-06 revision (and other documents) previously
carried "Stage 3/4" or similar mixed labels for `T66`+. That was an unauthorized informal
classification drift (2026-08-19 through 2026-08-21), **not** a project-owner-approved stage
transition — no `PreStageChecklist_Stage4` sign-off was ever produced, and
`IMPLEMENTATION_QUEUE.md`/`docs/Roadmap.md` never left their single "Stage 3 — Authentication &
Authorization" heading, under which `T41`–`T82` was always planned (Phases 0–6: seed/bootstrap data,
frontend, hardening & close-out). The project owner confirmed on 2026-08-21 that the project remains
formally **Stage 3**. This file now uses that terminology consistently throughout.
`docs/ImplementationLog/Stage4/Phase0.md`–`Phase7.md` is preserved **unchanged**, as a frozen
historical filing artifact of the drift period — its directory name is not evidence the project
reached a real Stage 4, and it is not renamed here (out of this reassessment's scope; see
`docs/ImplementationLog/Stage4/README.md` for the cross-reference). New phase logs are filed under
`docs/ImplementationLog/Stage3/Phase4.md` onward.

A high-level architectural maturity dashboard — one row per capability, grouped by category, so a
reader can see at a glance what exists, how solid it is, and what's still open. This is a snapshot
assessment, not a scoring formula; update it whenever a capability's status changes, alongside
[ProjectStatus.md](ProjectStatus.md) and [PROJECT_STATE.json](../PROJECT_STATE.json) (that broader
sync is explicitly deferred past this pass — see the note above). See
[DevelopmentGuide.md](DevelopmentGuide.md)'s "Documentation discipline" section for the project's
general rule that a stage isn't done until its documentation reflects reality — this file is itself
the counter-example that motivated this reassessment (see Overall Architecture Health, Documentation
Quality, below).

## Status legend

| Symbol | Meaning                                                        |
| ------ | -------------------------------------------------------------- |
| ✅     | Complete — built, tested, and (where applicable) verified live |
| 🟡     | Partial — exists but incomplete, unwired, or has a known gap   |
| ⏳     | Planned — scoped or configured, not yet built                  |
| ❌     | Not Started — no code exists                                   |
| ⚠      | Deprecated — superseded or reversed by a later decision        |

**Method note:** every row below was reassessed directly against current `main` (source, tests, ADRs,
`docs/ImplementationLog/`, `IMPLEMENTATION_QUEUE.md`, `PROJECT_STATE.json`) for this pass, not
inferred from "merged PR" or "task marked Done" alone. Rows unrelated to Stage 3's `T41`–`T82` work
(Core Architecture, most of Backend/Database/Infrastructure/Performance/AI) are unchanged from the
prior revision because nothing in that work touched them — reassessed and confirmed still accurate,
not skipped. Where evidence was insufficient to confirm or refute a claim, this file says
**Unverified** rather than assuming success.

---

## Core Architecture

Unchanged since Stage 1/Post-Stage-2 — none of Stage 3's `T41`–`T82` work touched this layer.
Reassessed against current `main` for this pass; every row below remains accurate.

| Capability                                                                               | Status | Stage        | Date Introduced | Notes                                                                                                                                                                                                                                                                  | Future Improvements                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------- | ------ | ------------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Clean Architecture layering (domain/application/infrastructure/presentation)             | ✅     | Stage 1      | 2026-08-05      | Mirrored on both backend and frontend, including Stage 3's auth work; see [ADR/0002](../ADR/0002-clean-architecture-layering.md).                                                                                                                                     | —                                                                                                                                                                                                                                                                      |
| Dependency Injection Container                                                           | ✅     | Stage 1      | 2026-08-05      | Hand-rolled `register`/`resolve`/`override`/`registered_interfaces`; see [ADR/0006](../ADR/0006-dependency-injection-container.md).                                                                                                                                    | `resolve()` has a check-then-act race if ever called off the event-loop thread (QA Q3, deferred to when a real `UnitOfWork` lands); no async-factory support (QA Q9, deferred).                                                                                        |
| Domain Foundation (`AggregateRoot`, `DomainEvent`, `Result[T,E]`)                        | ✅     | Stage 1      | 2026-08-05      | Pure domain primitives, no framework dependency.                                                                                                                                                                                                                       | —                                                                                                                                                                                                                                                                      |
| Repository Pattern (`AbstractRepository[T]`, `SqlAlchemyRepository[ModelT]`)             | ✅     | Stage 1      | 2026-08-05      | Generic; now proven against real business tables too — `SqlAlchemyUserRepository`/`RefreshTokenRepository`/`RolePermissionRepository` (Stage 3) extend it directly.                                                                                                  | `update()`'s "entity must already be attached to this session" contract is still undocumented and unenforced (F8, unapproved backlog).                                                                                                                                 |
| Base Service (CRUD convenience methods)                                                  | ✅     | Stage 1      | 2026-08-05      | `get_by_id_or_raise`, `list_page`, `create`, `update`, `delete`; reused directly by Stage 3's user-management routes (`T62`).                                                                                                                                          | `delete()` double-fetches (F9, low priority, unapproved).                                                                                                                                                                                                              |
| Event System (`EventBus`, `InMemoryEventBus`)                                            | ✅     | Stage 1      | 2026-08-05      | In-process publish/subscribe, multiple handlers per event type.                                                                                                                                                                                                        | —                                                                                                                                                                                                                                                                      |
| Command Bus (`CommandBus`, `InMemoryCommandBus`)                                         | ✅     | Post-Stage-2 | 2026-08-05      | Single-handler dispatch, returns `Result[R, AppError]`; see [ADR/0010](../ADR/0010-command-bus.md). Not used by Stage 3's auth routes (those call `AuthService`/repositories directly).                                                                               | Structurally near-identical to Query Bus/Event Bus with no shared base class — accepted trade-off (QA Q4), revisit only if a third single-handler bus is requested.                                                                                                    |
| Query Bus (`QueryBus`, `InMemoryQueryBus`)                                               | ✅     | Post-Stage-2 | 2026-08-05      | Symmetric sibling to Command Bus; see [ADR/0011](../ADR/0011-query-bus.md). Not used by Stage 3.                                                                                                                                                                       | Same as Command Bus above.                                                                                                                                                                                                                                             |
| Transaction Pipeline (`UnitOfWork`, `InMemoryUnitOfWork`, `TransactionPipelineBehavior`) | 🟡     | Post-Stage-2 | 2026-08-05      | `CommandBus` decorator; not exercised by Stage 3 (auth routes commit via `get_db()`, not this pipeline). See [ADR/0012](../ADR/0012-transaction-pipeline.md).                                                                                                          | Sequencing has never been exercised against a resource that can fail in interesting ways (QA Q2, deferred until a real `UnitOfWork` exists). If `rollback()` itself raises, the original exception loses primacy (QA Q7, same gate).                                    |
| Caching Abstraction (`Cache`, `InMemoryCache`)                                           | 🟡     | Post-Stage-2 | 2026-08-05      | Dict-backed, lazy TTL expiry via `time.monotonic()`; see [ADR/0013](../ADR/0013-caching-abstraction.md).                                                                                                                                                               | Zero callers today, including Stage 3's RBAC lookups (deliberately fresh-per-request, no caching policy — see Security section). Revisit once a real caller appears.                                                                                                    |
| Module Manifest Loader (`ModuleManifest`, `ModuleManifestLoader`)                        | 🟡     | Post-Stage-2 | 2026-08-05      | Closes the gap `ModuleRegistry`'s docstring left open; see [ADR/0014](../ADR/0014-module-manifest-loader.md).                                                                                                                                                          | Not wired into `main.py` — no real manifest exists yet. Unrestricted dynamic import via `importlib.import_module` with no allowlist (QA Q5, deferred until wired in).                                                                                                  |
| Plugin Architecture (`AppModule`, `ModuleRegistry`)                                      | 🟡     | Stage 1      | 2026-08-05      | Global registry empty; `main.py` calls `mount_all()` unconditionally (a no-op today).                                                                                                                                                                                  | Proven only via a throwaway test module — no real plugin exists yet.                                                                                                                                                                                                   |
| Workflow Engine (`WorkflowDefinition`, `WorkflowEngine`)                                 | 🟡     | Stage 1      | 2026-08-05      | Generic state machine, proven via a toy state graph.                                                                                                                                                                                                                   | No real workflow definitions exist yet (the schema's `workflow_definitions`/`workflow_states` tables are unwired).                                                                                                                                                     |
| Architecture Health Check (`check_container_health`, `assert_container_healthy`)         | ✅     | Post-Stage-2 | 2026-08-05      | Wired into the real app's startup path; resolves `IMPLEMENTATION_QUEUE.md`'s T15/F7. See [ADR/0015](../ADR/0015-architecture-health-check.md). Stage 3's boot-smoke-test checks (re-run across nearly every `T52`–`T78` batch) exercise this indirectly.               | —                                                                                                                                                                                                                                                                      |

## Backend

| Capability                                                                      | Status | Stage        | Date Introduced | Notes                                                                                                                                                                                                                                                                                                                                                     | Future Improvements                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------------- | ------ | ------------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FastAPI application skeleton                                                    | ✅     | Stage 0      | 2026-08-03      | App factory (`create_app()`), structured JSON logging, error handling.                                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                                                    |
| Validation Framework (`Validator[T]`, `validate_all()`)                         | ✅     | Stage 1      | 2026-08-05      | —                                                                                                                                                                                                                                                                                                                                                          | —                                                                                                                                                                                                                                                    |
| Pagination / Filtering / Sorting / Search query shapes                          | 🟡     | Stage 1      | 2026-08-05      | Still not interpreted anywhere. Stage 3's routes (`/auth/*`, `/users*`) don't need list/filter/sort, so this gap was neither closed nor exercised by that work.                                                                                                                                                                                          | Not yet interpreted — `SqlAlchemyRepository.list()` still only takes `limit`/`offset` (confirmed directly in `sqlalchemy_repository.py` this pass); `BaseService.list_page()`/CRUD router's list route still don't forward filters/sort (F2, unapproved). |
| Response Wrapper (`ApiResponse[T]`)                                             | ✅     | Stage 1      | 2026-08-05      | Now used for real — `GET /api/v1/auth/me` wraps `MeResponse` in `ApiResponse[T]` (`T61`); `login`/`refresh`/`logout` deliberately use bare response bodies instead (a token pair/void isn't a "fetchable resource").                                                                                                                                       | —                                                                                                                                                                                                                                                    |
| Base Controller / CRUD router factory (`build_crud_router`)                     | 🟡     | Stage 1      | 2026-08-05      | Still proven only against a test-only entity. Stage 3's `/api/v1/auth/*` and `/api/v1/users*` routes are **hand-written** (`presentation/api/v1/auth.py`, `users.py`), not built on this factory — the first real routes in the project deliberately didn't use it.                                                                                       | Still never mounted into the real app for a real entity — Stage 3 didn't change that.                                                                                                                                                                 |
| Persistence session lifecycle (`get_db()`)                                      | ✅     | Stage 1 / 3  | 2026-08-05      | **F1 resolved (Stage 3 Phase 0, `T42`/`T43`):** `get_db()` now commits on clean exit and rolls back on exception ([ADR/0020](../ADR/0020-session-commit-rollback-policy.md)) — confirmed directly in source this pass. Every Stage 3 write (user creation, role assignment, token issuance/revocation, bootstrap admin) depends on this fix.              | Deliberately still `except Exception`, not `BaseException` — doesn't yet cover `asyncio.CancelledError` (a scoped, documented limitation per ADR-0020, not an oversight). Cached engine (`get_engine()`) is still never disposed on shutdown (F3).       |
| API route surface (`/api/v1/auth/*`, `/api/v1/users*`)                          | ✅     | Stage 3      | 2026-08-13       | New since the prior revision. Eleven route/method combinations: `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`, `GET /auth/me` (`T58`–`T61`); `GET`/`POST /users`, `GET`/`PUT /users/{id}`, `POST /users/{id}/deactivate` (`T62`); `POST`/`DELETE /users/{id}/roles[/{role_id}]` (`T63`) — confirmed unchanged since `T63` by every subsequent batch's `app.openapi()["paths"]` check. Extensive integration-test coverage (login/refresh/logout/me/users/roles, 401 vs. 403, invalid/expired/revoked/unknown-token cases — `T64`).           | Still the entire route surface — no business-domain route exists yet (matters/clients/documents/etc.).                                                                                                                                                |
| First-admin bootstrap CLI (`bootstrap-admin`)                                   | ✅     | Stage 3      | 2026-08-17       | `infrastructure/cli/bootstrap.py`; `run_bootstrap()` is the testable core, `main()`/`_async_main()` the interactive entry point (`getpass`, never `argv`/env/config file — ADR-0018 D4). Idempotent; both the core and the entry point are covered by tests (`T67`/`T68`).                                                                                | Hand-rolls persistence instead of reusing `SqlAlchemyUserRepository.assign_role()` (non-blocking QA comment, functionally immaterial); the missing-`Administrator`-role `RuntimeError` guard has zero test coverage.                                    |
| Background Job Framework (`Job`, `JobQueue`, `InMemoryJobQueue`, `JobRegistry`) | 🟡     | Stage 1      | 2026-08-05      | Unchanged — asyncio-task-backed execution + status tracking.                                                                                                                                                                                                                                                                                              | Only `NoOpJob` exists — no real job has been built yet.                                                                                                                                                                                               |
| File Storage Abstraction (`FileStorage`, `LocalFileStorage`)                    | ✅     | Stage 1      | 2026-08-05      | Path-traversal-safe filesystem implementation. Not used by Stage 3.                                                                                                                                                                                                                                                                                        | Real backend (S3, etc.) deferred until a feature needs one.                                                                                                                                                                                          |
| Notification Framework (`Notifier`, `LoggingNotifier`)                          | 🟡     | Stage 1      | 2026-08-05      | Server-side; logs instead of sending. Not used by Stage 3 — no email verification, password-reset, or notification flow exists (explicitly out of scope for Stage 3).                                                                                                                                                                                     | No real channel (email/SMS/push) implemented yet.                                                                                                                                                                                                    |
| Search Foundation (`SearchIndex`, `InMemorySearchIndex`)                        | 🟡     | Stage 1      | 2026-08-05      | Unchanged — naive substring/filter match, proves the port.                                                                                                                                                                                                                                                                                                 | Real full-text/OCR/smart search deferred; the Stage 2 schema's GIN full-text index still isn't wired to this port.                                                                                                                                    |
| Feature Flags (`FeatureFlagProvider`, env-driven)                               | ✅     | Stage 1      | 2026-08-05      | Unchanged.                                                                                                                                                                                                                                                                                                                                                  | —                                                                                                                                                                                                                                                    |
| Performance Metrics Service (`MetricsService`, `LoggingMetricsService`)         | 🟡     | Post-Stage-2 | 2026-08-05      | Unchanged — not instrumented into the auth routes or any dispatch path.                                                                                                                                                                                                                                                                                    | No real backend wired; not instrumented into any request path.                                                                                                                                                                                        |
| CRUD router query-parameter exposure                                            | ❌     | —            | —               | Unchanged.                                                                                                                                                                                                                                                                                                                                                  | Depends on the Pagination/Filtering framework above being wired first (F2 → T8, unapproved).                                                                                                                                                         |

## Frontend

| Capability                                                                                                 | Status | Stage   | Date Introduced | Notes                                                                                                                                                                                                                                                                                                                                                          | Future Improvements                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------------ | ------ | ------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Electron shell (main process, secure preload, IPC scaffold)                                                | ✅     | Stage 0 | 2026-08-03      | The IPC scaffold now carries real functionality: `AUTH_STORE_REFRESH_TOKEN`/`AUTH_GET_REFRESH_TOKEN`/`AUTH_CLEAR_REFRESH_TOKEN` channels, `safeStorage`-backed encrypted file persistence in the main process (`electron/main.ts`, `T71`). Security baseline (`contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, no generic passthrough) confirmed unchanged.                            | Packaging (`electron-builder`) still never built or tested end-to-end — see Infrastructure/Deployment.                                                                                                       |
| React + Vite + Tailwind + shadcn/ui foundation                                                             | ✅     | Stage 0 | 2026-08-03      | Unchanged.                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                                                            |
| Frontend↔backend E2E proof (`HealthCheckPage`)                                                             | ✅     | Stage 0 | 2026-08-03      | `HealthCheckPage` itself unchanged; in practice superseded as the project's E2E proof point by the full login → protected-route → logout live walkthrough `T79` performed against a running backend/frontend (2026-08-20).                                                                                                                                    | —                                                                                                                                                                                                            |
| `Result<T, E>` discriminated union                                                                         | ✅     | Stage 1 | 2026-08-05      | Unchanged.                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                                                            |
| Pagination/query TS types (`PageRequest`, `PaginatedResponse<T>`, `SortSpec`, `FilterSpec`, `SearchQuery`) | 🟡     | Stage 1 | 2026-08-05      | Unchanged, still unexercised — Stage 3's routes don't need pagination.                                                                                                                                                                                                                                                                                          | Frontend types still exist ahead of the backend actually interpreting them (see Backend's Pagination row).                                                                                                   |
| HTTP Client (`httpClient.ts`)                                                                              | ✅     | Stage 3 | 2026-08-18       | **F10 resolved.** `post`/`put`/`delete` added alongside `get()` (`T69`); `HttpError` now carries the backend's structured `{"error":{"code","message"}}` body via `buildHttpError()`/`isStructuredErrorBody()`, falling back to a generic message only on a shape mismatch. Global `Authorization: Bearer <token>` header attachment and global 401 handling (clear session, `ipcBridge.clearRefreshToken()`, notify a registered handler) added (`T74`) — confirmed directly in source this pass (`httpClient.ts:64-95`). The pre-existing `204 No Content` parsing defect (flagged since `T70`) is resolved (`response.status === 204` returns early, confirmed at line 90). No automatic refresh-token exchange/retry/rotation on 401 — explicitly prohibited by `T74`'s authorized scope, not a gap. | —                                                                                                                                                                                                            |
| UI component library (shadcn/ui)                                                                           | 🟡     | Stage 0 | 2026-08-03      | Still only `Button` is shadcn CLI-sourced; `LoginPage`, `ProtectedRoute`, and an updated `MainLayout` header are hand-authored, following the same pattern.                                                                                                                                                                                                     | shadcn CLI still broken on this Windows setup (see [KnownIssues.md](KnownIssues.md)) — unchanged.                                                                                                             |
| Frontend routing, including Protected-route wrapper                                                        | ✅     | Stage 3 | 2026-08-20       | `react-router-dom` SPA routing (unchanged) plus a new `ProtectedRoute` component (`frontend/src/app/ProtectedRoute.tsx`, `T73`): redirects to `/login` via `<Navigate replace>` when `useAuth().currentUser` is `null`, otherwise renders `<Outlet/>` — confirmed directly in source this pass.                                                              | `<Navigate replace>` semantics verified only by source inspection, not automated-tested — a named, still-open non-blocking QA finding from `T73`.                                                            |
| Auth state management (`AuthProvider`/`auth.ts`)                                                            | 🟡     | Stage 3 | 2026-08-19       | New. React Context (`frontend/src/app/providers/AuthProvider.tsx`) holding `currentUser`/`tokens`, `login()`/`logout()`; registers `httpClient.ts`'s global 401 handler (`T70`). **Governance history, preserved:** the required approval-checkpoint pause between authorization and implementation was skipped for this batch (~5 seconds apart) — a permanent, disclosed process finding, not a technical defect.                                        | Has no mount-time session-restoration effect — see the Security section's Electron IPC row below; every fresh app launch starts logged out even when a persisted refresh token exists.                       |
| Login page/form (`LoginPage.tsx`)                                                                          | ✅     | Stage 3 | 2026-08-19       | New. Integrates `AuthProvider.login()`; on success inside Electron, persists the refresh token via `ipcBridge.setRefreshToken(tokens.refresh_token)` — confirmed directly in source this pass. QA Decision and a second, informal "Independent Technical Verification" pass both **Approved with comments**.                                                 | Non-blocking IPC-persistence test-coverage gap recorded by both review passes, still open.                                                                                                                    |
| Current-user display + logout (`MainLayout` header)                                                        | ✅     | Stage 3 | 2026-08-20       | New. Displays the authenticated user in `MainLayout`'s header; wires `ipcBridge.clearRefreshToken()` into the user-facing logout flow (`T75`), completing `T74`'s deferred work.                                                                                                                                                                              | —                                                                                                                                                                                                            |
| Frontend test setup (Vitest + React Testing Library)                                                       | ✅     | Ongoing | 2026-08-03      | Grown from 9 tests (Stage 0, 3 files) to **43/43 passing** across 8 files (`result`, `query`, `HealthCheckPage`, `httpClient`, `AuthProvider`, `LoginPage`, `ProtectedRoute`, `MainLayout`) — last independently re-run and confirmed 2026-08-20 (`T79`). **Not re-run during this documentation-only pass** (`T80`'s authorized scope permits no code/test execution changes); figure carried forward from the last live verification, not re-derived. | —                                                                                                                                                                                                            |

## Security

**This section carries the most significant changes since the prior revision** — Stage 3 is this
project's first real authentication/authorization implementation. Reassessed directly against
current `main`'s source and tests, not inferred from task-completion status.

| Capability                                                                         | Status | Stage             | Date Introduced | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Future Improvements                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------ | ------ | ------------------ | --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Security foundation placeholders                                                   | ⚠      | Stage 0            | 2026-08-03      | See [ADR/0004](../ADR/0004-security-foundation-placeholders.md) for the original placeholder design. Superseded in practice by the real Stage 3 implementation below — kept here as historical record of the starting point, not current-state.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                                                                                                                                                                                                                                                          |
| Authentication Framework (backend + frontend, end-to-end)                          | 🟡     | Stage 3            | 2026-08-05       | Real, tested, live-verified for the browser/HTTP surface: Argon2id password hashing (D2, `T46`), PyJWT access tokens + DB-backed revocable refresh tokens (D1, `T47`/`T49`), `AuthService`/`JwtAuthenticationProvider` (`T50`–`T52`), `login`/`refresh`/`logout`/`me` routes (`T58`–`T61`), frontend `AuthProvider`/`LoginPage`/`ProtectedRoute`/global-401-handling/current-user-display+logout (`T70`, `T72`–`T75`). `T79` (2026-08-20) independently re-verified live, against a running backend + frontend, the full unauthenticated-redirect → login → protected-access → logout → cleared-state walkthrough. **Partial, not complete, for one architecturally significant reason:** Electron session-restoration-on-launch (ADR-0018 D6's whole point for the desktop target) does not exist — see the dedicated row below. Not marked ✅ for that reason, not an oversight.                             | Implement `ipcBridge.getRefreshToken()` + an `AuthProvider` mount-time restoration effect + a refresh-token-exchange call, then verify live inside an actual Electron window (`T82`, reserved, **not authorized**). Further hardening beyond `T78` remains explicitly unauthorized.                        |
| Electron session-restoration-on-launch (refresh-token consumption via IPC)         | ❌     | Stage 3            | —                | **Known issue — explicitly not resolved, verified directly against current `main` for this pass, not assumed from the prior record.** `electron/preload.ts` exposes `getRefreshToken()` (IPC channel `AUTH_GET_REFRESH_TOKEN`, confirmed present, `electron/main.ts:74-84`); `frontend/src/infrastructure/ipc/ipcBridge.ts`'s `ElectronApi` interface **does not declare or expose it** (confirmed — only `getAppInfo`/`setRefreshToken`/`clearRefreshToken` are surfaced); `AuthProvider.tsx` has **no mount-time effect** that calls it (confirmed — its only `useEffect` registers the 401 handler). The storage *write* and *clear* paths are genuinely wired (`LoginPage.tsx` calls `ipcBridge.setRefreshToken()` on login; `httpClient.ts`/`AuthProvider.tsx` call `ipcBridge.clearRefreshToken()` on 401/logout) — only the *read-back-on-launch* path is missing. Practical effect: every fresh Electron launch starts logged out, even with a valid persisted refresh token on disk. Recorded in `PROJECT_STATE.json`'s `knownIssues` as `electron-refresh-token-not-consumed`, found by `T79`'s follow-up pass (2026-08-20) as a **static-analysis-only** observation — `T79`'s tooling could drive a browser tab but not an actual Electron `BrowserWindow`, so this has never been runtime-verified either way. **Not fixed as part of this reassessment** (`T80` is documentation-only) and **not authorized for a fix** — that is `T82`'s scope, and `T82` is reserved but not authorized. | The natural fix — surface `getRefreshToken()`, add a mount-time restoration effect, exchange the stored refresh token for a new access token on launch — is unimplemented and unauthorized. Once implemented, needs live verification inside an actual Electron window (`T82`), not a browser tab.        |
| Authorization Framework (`AuthorizationService`, `RbacAuthorizationService`, `RequirePermission`) | ✅     | Stage 3            | 2026-08-08       | **F11 resolved.** `RbacAuthorizationService` + `RolePermissionRepository` are real (`T53`); `RequirePermission(*permissions: str)` is a real, reusable FastAPI dependency gating every route past `health`/`version` (`T54`), extended to OR-across-multiple-permissions semantics (`T57` established the 401-vs-403 distinction; `T63` extended to multiple permission codes). Denials are audited (`T65`). 59 real `role_permission` associations seeded against the project-owner-approved matrix across 6 roles, exact-match tested (`T66`). First-admin bootstrap closes the chicken-and-egg problem of who assigns the first role (`T67`/`T68`).                                                                                     | Proven only at the identity/access-control level (`users`/`roles`/`permissions`) — no business table or business permission code exists yet to exercise it against a real resource. RBAC mapping is fresh-per-request, no caching policy (deliberate, per `T55`'s authorization).                            |
| Audit Logging Framework (`AuditLogger`, `LoggingAuditLogger`, `audit_logs` table)  | ✅     | Stage 1 / 2 / 3    | 2026-08-05       | Now has real callers beyond schema: `AuthService.authenticate()` records `login_success`/`login_failure` per call; `RequirePermission`'s final-candidate denial records `permission_denied` (`T65`) — the existing, unmodified `LoggingAuditLogger`. See [ADR/0007](../ADR/0007-audit-logging-without-database-table.md) (superseded) and [ADR/0009](../ADR/0009-audit-logs-table-reverses-adr-0007.md).                                                                                                                                                                                                                                                                                              | —                                                                                                                                                                                                                                                                                                          |
| CORS configuration                                                                 | ✅     | Stage 0 / 3        | 2026-08-20       | **F5 resolved.** Wildcards replaced with an explicit allow-list — confirmed directly in `main.py`: `allow_methods=["GET","POST","PUT","DELETE"]`, `allow_headers=["Content-Type","Authorization"]` (`T78`). 10/10 new tests (`test_cors_configuration.py`).                                                                                                                                                                                                                                                                                                                                                                                                                                              | —                                                                                                                                                                                                                                                                                                          |
| API docs exposure (`/docs`, `/redoc`, `/openapi.json`)                             | 🟡     | Stage 0 / 3        | 2026-08-20       | **F4 mostly resolved.** `/docs`/`/redoc` now gated behind `settings.is_development` — confirmed directly in `main.py` (`docs_url="/docs" if settings.is_development else None`) (`T77`). `/openapi.json` was **deliberately left ungated** — a named, explicit deferral inside `T77`'s own authorized scope, not an oversight — and still exposes the full API schema unconditionally in every environment.                                                                                                                                                                                                                                                                                            | Gate `/openapi.json` too, if ever desired — explicitly named as Deferred Work by `T77`, not authorized or scheduled.                                                                                                                                                                                        |
| Module manifest dynamic import                                                     | 🟡     | Post-Stage-2      | 2026-08-05       | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | No live attack surface today (not wired into `main.py`) — needs an allowlist prefix check once it is (QA Q5, deferred).                                                                                                                                                                                    |
| Metrics tag redaction                                                              | ✅     | Post-Stage-2      | 2026-08-06       | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —                                                                                                                                                                                                                                                                                                          |
| Row-level / column-level access control                                            | ❌     | —                 | —               | Unchanged — no business feature exists yet to need it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Only once a real business permission model and real business routes exist.                                                                                                                                                                                                                                |
| Electron secure token storage IPC handlers (`T71` itself)                          | 🟡     | Stage 3            | 2026-08-19       | `safeStorage`-backed encrypted file persistence for the refresh token in the Electron main process (`electron/main.ts`), IPC-exposed via `preload.ts`, per ADR-0018 D6. **Ships with zero tests and no manual verification** — QA's own disposition on `T71` was "Approved with comments," the three comments being exactly: no tests, no manual verification, default file permissions. Not a code defect, but a real, currently-unclosed test-coverage and verification gap on a security-sensitive code path.                                                                                                                                                                                       | Add unit/integration test coverage for the three IPC handlers (`AUTH_STORE`/`GET`/`CLEAR_REFRESH_TOKEN`); consider explicit file-permission hardening on `refresh-token.enc` beyond OS defaults.                                                                                                          |

## Database

| Capability                                                               | Status | Stage   | Date Introduced | Notes                                                                                                                                                           | Future Improvements                                                                               |
| ------------------------------------------------------------------------ | ------ | ------- | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Schema design (49 tables, 12 migrations, 11 domain sections + seed data) | ✅     | Stage 2 | 2026-08-05      | Unchanged — pure schema (Stage 3 added no new tables of its own beyond `refresh_tokens`, `T49`). See [Database.md](Database.md) and [ERD.md](ERD.md).            | —                                                                                                 |
| Persistence-layer ORM models (not domain entities)                       | ✅     | Stage 2 | 2026-08-05      | Unchanged. See [ADR/0008](../ADR/0008-persistence-models-not-domain-entities.md).                                                                                | No `relationship()` navigation declared yet — deliberate, until a feature needs it.               |
| Soft delete / audit columns (`AuditMixin`)                               | ✅     | Stage 2 | 2026-08-05      | Unchanged.                                                                                                                                                       | —                                                                                                 |
| Optimistic locking (`OptimisticLockMixin`)                               | ✅     | Stage 2 | 2026-08-05      | Unchanged.                                                                                                                                                       | —                                                                                                 |
| Full-text search index (GIN, OCR'd text)                                 | ✅     | Stage 2 | 2026-08-05      | Unchanged — still not wired to any feature.                                                                                                                      | Not wired to the `SearchIndex` port or any feature yet.                                           |
| Seed lookup data                                                         | ✅     | Stage 2 / 3 | 2026-08-17  | Original Stage 2 seed data, plus 59 `role_permission` associations against the owner-approved matrix (`T66`) layered on top.                                     | —                                                                                                 |
| Migration chain integrity check                                         | ❌     | —       | —               | Still no automated check. Every individual Stage 3 batch that added a migration manually confirmed exactly one Alembic head as part of its own closeout (e.g. `T66`'s `224b650e5235`) — real but ad hoc, not the programmatic guarantee F6 asks for.             | F6, unapproved — add a test asserting `ScriptDirectory` reports exactly one head.                 |
| Repository/service/route wiring to the schema                           | 🟡     | Stage 3 | 2026-08-15       | **Materially changed from the prior "zero tables wired" assessment.** The identity/access-control tables (`users`, `roles`, `permissions`, `role_permissions`, `refresh_tokens`) are now genuinely wired end-to-end — real repositories (`SqlAlchemyUserRepository`, `RefreshTokenRepository`, `SqlAlchemyRolePermissionRepository`), a real service (`AuthService`), real routes (`/api/v1/auth/*`, `/api/v1/users*`), tested against live Postgres. The other **~44 business tables** (matters, clients, properties, documents, financial, scheduling, OCR/QR/backups, etc.) remain completely unwired — confirmed by inspection, no repository/service/route touches them. `PROJECT_STATE.json`'s `overallProjectPercent` is still deliberately `0` — consistent with treating identity/access-control plumbing as an infrastructure precondition, not a business feature; not an inconsistency to "fix." | Wiring a real business feature (repository → service → route) to the remaining schema is still the largest piece of unscoped Stage-4+ work — unchanged in kind, though the auth vertical slice is now a concrete template for how to do it. |

## Infrastructure

| Capability                                                    | Status | Stage             | Date Introduced | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                          | Future Improvements                                                                                                                                                   |
| --------------------------------------------------------------- | ------ | ------------------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local dev environment (Docker Compose Postgres)               | ✅     | Stage 0           | 2026-08-03      | Unchanged. Repeatedly used and confirmed reachable across most (not all) Stage 3 batch closeouts.                                                                                                                                                                                                                                                                                                                                              | Not available in every environment — see [KnownIssues.md](KnownIssues.md).                                                                                            |
| Dependency management (`uv` backend, `npm` frontend)          | ✅     | Stage 0           | 2026-08-03      | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                     |
| Structured logging                                            | ✅     | Stage 0           | 2026-08-03      | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                     |
| Config management (`Settings`, pydantic-settings)             | ✅     | Stage 0 / 1       | 2026-08-03      | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                     |
| CI/CD pipeline                                                | ✅     | Stage 2.7 / 3      | 2026-08-16      | **Upgraded from 🟡 — the prior "no real GitHub Actions run has been observed yet" concern (T35) is resolved**, not by a dedicated task but by routine use: dozens of Stage 3 PRs (e.g. `T61`, `T62`, `T63`) each independently confirm "6/6 CI checks green" via `gh pr view`, directly cited in `IMPLEMENTATION_QUEUE.md`'s own batch records. `backend.yml`/`frontend.yml`/`release.yml` — see [ADR/0017](../ADR/0017-github-actions-ci.md). | Integration tests (Postgres service container), Docker image build, and deployment/packaging automation remain explicitly deferred per ADR-0017 — unchanged.          |
| Electron packaging (`electron-builder.yml`)                   | ⏳     | Stage 0           | 2026-08-03      | Config unchanged. The Electron main process now carries real functionality (auth IPC, `T71`) well beyond the Stage 0 shell, but the packaged build itself is still never built or tested end-to-end.                                                                                                                                                                                                                                          | Never actually built or tested end-to-end — see Deployment section.                                                                                                   |
| Backend distribution strategy (inside the Electron installer) | ❌     | —                 | —               | Unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                       | Options not yet evaluated — see [FutureIdeas.md](FutureIdeas.md).                                                                                                     |

## Quality

| Capability                              | Status | Stage        | Date Introduced | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Future Improvements                                                                                                                                                         |
| ---------------------------------------- | ------ | ------------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend test suite (pytest)             | ✅     | Ongoing      | 2026-08-03      | Grown from 282 to **506/506 passing** through Stage 3 — last independently re-run live against Postgres and confirmed 2026-08-20 (`T79`). **Not re-run during this documentation-only pass** — `T80`'s authorized scope permits no test execution or code change; figure carried forward, not re-derived.                                                                                                                                                                                                    | Keep re-running the full suite (not just unit) whenever Postgres is available — see [KnownIssues.md](KnownIssues.md).                                                       |
| Frontend test suite (Vitest + RTL)      | ✅     | Ongoing      | 2026-08-03      | Grown from 9 to **43/43 passing**, same last-verified date and same "not re-run this pass" caveat as above.                                                                                                                                                                                                                                                                                                                                                                                                | —                                                                                                                                                                           |
| Linting (ruff, black, eslint, prettier) | ✅     | Ongoing      | 2026-08-03      | Clean project-wide as of the last live check, 2026-08-20 (`T79`). Not independently re-run this pass.                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                                                                                           |
| ADR discipline                          | ✅     | Ongoing      | 2026-08-03      | Now **20 ADRs** (0001–0020), including three new Stage 3 records: [ADR/0018](../ADR/0018-authentication-authorization-architecture.md) (D1–D6), [ADR/0019](../ADR/0019-authentication-provider-interface-change.md) (D7), [ADR/0020](../ADR/0020-session-commit-rollback-policy.md) (`get_db()` policy). See [`/ADR`](../ADR/).                                                                                                                                                                          | —                                                                                                                                                                           |
| Documentation discipline                | 🟡     | Ongoing      | 2026-08-06 → 2026-08-22 | **Downgraded from ✅ — this file is itself the counter-example that motivated `T80`.** This scorecard was last updated 2026-08-06, before any of `T41`–`T82` existed, while `PROJECT_STATE.json`/`IMPLEMENTATION_QUEUE.md`/`docs/AI_HANDOVER.md`/`docs/ImplementationLog/` all stayed substantially current throughout Stage 3 — the single most "at a glance" document was also the most stale one for over six weeks. A stage-numbering inconsistency (`IMPLEMENTATION_QUEUE.md`/`docs/Roadmap.md` saying "Stage 3," `PROJECT_STATE.json`/`docs/ImplementationLog/` saying "Stage 4," for the same `T66`+ work) also persisted uncorrected from 2026-08-19 to 2026-08-21. | Known documentation debt not fixed by this pass (outside `T80`'s narrowed scope): `docs/ImplementationLog/Stage4/Phase7.md`'s metadata block was never updated to reflect its `T78` batch; `T78`/`T79` have no formal phase-log entries; the broader sync of `docs/Architecture.md`/`docs/API.md`/`docs/FeatureRegistry.md`/`docs/ProjectStatus.md`/`PROJECT_STATE.json`/`CHANGELOG.md`/`docs/SessionReport.md` remains deferred. |
| QA review process                       | ✅     | Ongoing      | 2026-08-06 → 2026-08-22 | Stage 3 established a real per-batch QA Decision discipline (`Approved`/`Approved with comments`/`Rework required`), recorded for nearly every batch `T52`–`T78`. An authorization-recording-before-implementation governance gap recurred across four consecutive batches (`T52`–`T55`) before being corrected and held consistently from `T56` onward. An informal, undocumented second review step ("Independent Technical Verification," used for `T72`/`T73`) cited a governing document that does not exist (`Legal_DMS_Process_Supervision.md`) — resolved 2026-08-22: the project owner decided not to formally adopt this role, and the dangling citation was corrected at its source. A "Frontend Developer" role, in routine use since `T69`, was formally adopted the same day (`docs/prompts/FrontendDeveloper.md`). | Re-classify Q2/Q3/Q5/Q7/Q9 whenever their gating dependency lands (unchanged from before Stage 3).                                                                          |

## Performance

Unchanged since the prior revision — none of Stage 3's work touched query execution, caching, or
connection pooling.

| Capability                                                 | Status | Stage        | Date Introduced | Notes                                                                                               | Future Improvements                                                                                       |
| ----------------------------------------------------------- | ------ | ------------ | --------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| Query framework execution (filter/sort translation to SQL) | ❌     | —            | —               | Unchanged — `SqlAlchemyRepository.list()` still doesn't interpret `FilterSpec`/`SortSpec` (confirmed this pass). | F2/T4–T10, largest item in the unapproved Stage 2.5 backlog.                                              |
| Caching                                                    | 🟡     | Post-Stage-2 | 2026-08-05      | Unchanged — `InMemoryCache` exists, zero callers, including Stage 3's RBAC lookups (deliberately uncached).  | Move to a real backend (e.g. Redis) once a hot-path caller appears — accepted trade-off, not scheduled.   |
| Performance metrics instrumentation                        | 🟡     | Post-Stage-2 | 2026-08-05      | Unchanged — not instrumented into the new auth routes either.                                          | Not instrumented into any dispatch path, middleware, or route yet.                                        |
| Connection pool tuning                                     | ❌     | —            | —               | Unchanged.                                                                                              | F12 — deliberately deferred until a real workload exists to size against.                                 |
| DI container async factory support                         | ❌     | —            | —               | Unchanged.                                                                                              | QA Q9 — only needed once a real async-requiring implementation is proposed.                                |

## AI

Unchanged — no AI feature work has occurred.

| Capability                                                        | Status | Stage   | Date Introduced | Notes                                                                 | Future Improvements                                                                                         |
| ----------------------------------------------------------------- | ------ | ------- | --------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| AI request/response schema (`ai_requests`, `ai_responses` tables) | 🟡     | Stage 2 | 2026-08-05      | Unchanged.                                                             | Documented as intentionally incomplete pending a real AI feature decision, not a gap.                       |
| AI feature implementation                                         | ❌     | —       | —               | Unchanged.                                                             | Explicitly out of scope until Stage 4+, and only with explicit project-owner direction.                     |

## Deployment

| Capability                               | Status | Stage   | Date Introduced | Notes                                                                                                     | Future Improvements                                                                                      |
| ------------------------------------------ | ------ | ------- | --------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Electron desktop packaging               | ⏳     | Stage 0 | 2026-08-03      | Unchanged.                                                                                                       | Never built or tested end-to-end — see Infrastructure section.                                           |
| Production environment configuration     | 🟡     | Stage 0 / 3 | 2026-08-20  | **F4 mostly resolved** — `/docs`/`/redoc` now gated on `settings.environment`-derived `is_development` (`T77`). `/openapi.json` remains ungated in every environment, deliberately deferred.                            | Gate `/openapi.json`, if ever desired — not authorized (see Security section).                          |
| Database migration strategy (production) | 🟡     | Stage 2 | 2026-08-05      | Unchanged.                                                                                                       | No documented production migration runbook exists yet.                                                    |
| Backend service deployment strategy      | ❌     | —       | —               | Unchanged.                                                                                                       | See Infrastructure section's "Backend distribution strategy."                                            |
| Monitoring / observability backend       | ❌     | —       | —               | Unchanged.                                                                                                       | Only worth building once there's a deployed instance to observe.                                          |

---

## Overall Architecture Health

Qualitative assessment as of 2026-08-22, reassessed directly against `PROJECT_STATE.json`,
`IMPLEMENTATION_QUEUE.md`, current source, tests, ADRs, and `docs/ImplementationLog/` — not a rollover
of the 2026-08-06 assessment's wording. Not a formula — a snapshot judgment call, meant to be revised
each time this file is updated.

| Dimension                     | Assessment                                                                                        | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Architecture Completeness** | 🟡 High for framework/scaffolding and for the identity/access-control vertical slice; 0% for business features | The Stage 0–2 framework layer is unchanged and complete for what it scopes. Stage 3 adds this project's **first real, end-to-end, tested, live-verified vertical feature** — authentication + RBAC, backend and frontend, including a live desktop-shell IPC surface — a genuine architectural milestone, not just more scaffolding. But it stops at the identity/access-control tables; `overallProjectPercent` is still, correctly, `0` in `PROJECT_STATE.json` — no business feature (matters/clients/documents/etc.) has been wired to the Stage 2 schema, by design. The Electron session-restoration gap (see Security) means even the auth slice isn't complete on its intended target runtime. |
| **Documentation Quality**     | 🟡 Downgraded from ✅                                                                                | This file's own staleness (unchanged for six weeks across `T41`–`T79`) is the headline finding of this reassessment. Other project documents (`PROJECT_STATE.json`, `IMPLEMENTATION_QUEUE.md`, `docs/AI_HANDOVER.md`, `docs/ImplementationLog/`) stayed substantially current and unusually well-disclosed (governance gaps are recorded, not hidden) throughout the same period — the discipline exists elsewhere in the project, it just didn't reach this file until now. A stage-numbering inconsistency (Stage 3 vs. "Stage 4") also went uncorrected for roughly a month. Known remaining debt: `Phase7.md`'s metadata gap, missing `T78`/`T79` phase logs, and the broader `T80` bundle (Architecture.md/API.md/FeatureRegistry.md/ProjectStatus.md/PROJECT_STATE.json/CHANGELOG.md/SessionReport.md) still deferred past this pass.                                                                                                                                                                     |
| **Technical Debt**            | 🟡 Lower than before, still explicitly tracked                                                       | Six of the twelve original Stage 2.5 findings (`F1`–`F12`, confirmed against `IMPLEMENTATION_QUEUE.md`'s own findings table this pass) are now resolved: **F7** (`configure_container()` factories never resolved at startup) was already resolved pre-Stage-3, via the post-Stage-2 Architecture Health Check (`T15`/ADR-0015) — already marked `~~Resolved~~` in that table, not new to this reassessment. Five more are resolved by Stage 3 work specifically: **F1** (`get_db()` never committed — Critical/P0, confirmed fixed via ADR-0020, verified directly against `session.py`'s `get_db()` this pass), **F4** (`/docs`/`/redoc` unconditional — `T77`), **F5** (CORS wildcards — `T78`), **F10** (`httpClient.ts` generic errors — `T69`), and **F11** (no `RequirePermission` — `T54`). The remaining six — **F2, F3, F6, F8, F9, F12** — are still open, all low-to-medium priority, still unapproved. (6 resolved + 6 open = 12, the original total.) **New debt surfaced by this reassessment:** the Electron refresh-token-not-consumed gap (architecturally significant — it undercuts ADR-0018 D6's entire premise for the desktop target, not a minor polish item); `T71`'s zero test coverage on a security-sensitive IPC surface; `T78`'s incomplete phase-log metadata; the now-resolved but real four-batch authorization-recording governance lapse (`T52`–`T55`).                                                                             |
| **Code Quality**               | ✅ Clean, per last live verification                                                                 | `ruff`/`black`/`eslint`/`prettier` all clean as of 2026-08-20 (`T79`) — not independently re-verified during this documentation-only pass, disclosed rather than assumed. Consistent Clean Architecture layering held throughout Stage 3's additions.                                                                                                                                                                                                                                                                                |
| **Test Coverage**              | 🟡 Strong in breadth, with real, named gaps on the newest and most security-sensitive surface        | 506 backend + 43 frontend tests, last live-verified 2026-08-20, not re-run this pass. Breadth is genuinely strong — every auth/RBAC route has positive, negative, 401-vs-403, and invalid/expired/revoked-token coverage. But: `T71` (Electron secure-storage IPC) has **zero** test coverage; the Electron session-restoration behavior has **never** been runtime-verified in its actual target environment (only a browser tab, which architecturally cannot exercise it); `T67`'s missing-role `RuntimeError` guard is untested; `T72`'s IPC-persistence path has a recorded, still-open test-coverage gap.                                                                                     |
| **Scalability**                | 🟡 Deliberately deferred, seams in place                                                             | Unchanged from before — every default implementation stays explicitly single-instance by design; RBAC lookups are deliberately uncached per-request. No connection-pool tuning yet.                                                                                                                                                                                                                                                                                                                                                  |
| **Maintainability**            | ✅ Strong, with disclosed process blemishes now largely self-corrected                               | ADRs and phase logs consistently explain *why*; the project's own governance discipline caught and disclosed its own lapses (the `T52`–`T55` authorization-recording gap, the undocumented "Independent Technical Verification" step, the Frontend Developer role gap) rather than hiding them, and resolved most of them by 2026-08-22. The exception proving this pattern isn't universal is this scorecard's own six-week staleness, corrected only by this reassessment.                                                     |
| **Security**                   | 🟡 Real progress, with one architecturally significant unresolved gap                                | No longer "placeholder-stage": Argon2id hashing, revocable JWT refresh tokens, RBAC enforcement on every real route, audit logging of auth events, tightened CORS, gated API docs, and a live-verified browser walkthrough are all real and tested. **But:** the Electron-specific session-persistence mechanism ADR-0018 D6 was designed around is not actually consumed anywhere in the frontend — a static-analysis finding, never runtime-verified, not fixed, not authorized for a fix (`T82`). `T71`'s IPC handlers (the code that reads/writes the encrypted token file) have no tests and were never manually verified. `/openapi.json` remains ungated in every environment, a named deliberate deferral. None of this is fabricated or inferred — each claim above was independently re-confirmed against current source this pass, not carried over from a prior record.                                          |
| **Developer Experience**       | 🟡 Strong chain, undercut by this file's own staleness until now                                     | `AI_BOOTSTRAP.md` → `PROJECT_STATE.json` → `docs/AI_HANDOVER.md` → `PROJECT_CHECKPOINT.md` gives a fast, accurate, honestly-disclosed "where are we" answer, and did so throughout Stage 3 even while this file lagged — a fresh session should trust those over a stale-looking dashboard. This file's correction here closes that specific gap; whether the discipline holds going forward (i.e., this file gets updated alongside future capability changes, per its own stated purpose) is not something this pass can verify in advance.                                                                                                                                                          |

---

_This file is part of the project's documentation set — keep it current alongside
[ProjectStatus.md](ProjectStatus.md) and [PROJECT_STATE.json](../PROJECT_STATE.json) whenever a
capability's status changes. See [AI_HANDOVER.md](AI_HANDOVER.md) for the deep handover doc this
scorecard summarizes, and [PROJECT_CHECKPOINT.md](../PROJECT_CHECKPOINT.md) for the fullest current
state detail as of 2026-08-21._
