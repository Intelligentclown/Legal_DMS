# Used by docker-compose.yml to provision local Postgres.
# Copy to .env and adjust if needed. Not read by the backend directly —
# backend/.env.example defines the DATABASE_URL the app itself uses.

POSTGRES_USER=legal_dms
POSTGRES_PASSWORD=legal_dms
POSTGRES_DB=legal_dms_dev
POSTGRES_PORT=5432
