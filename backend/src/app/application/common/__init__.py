"""Reusable application-layer building blocks: base service, pagination,
query (sort/filter/search) shapes, and the validation framework. Not ports
(those are in `application/interfaces/`) — concrete, reusable code every
future use case can build on directly.
"""
